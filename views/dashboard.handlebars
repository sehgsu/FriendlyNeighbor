<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">My Dashboard</h5>
          <h4>{{Users.first_name. Users.last_name}}</h4>
          <p>
            <strong>Name</strong>: {{Users.first_name. Users.last_name}}</p>
          <p>
            <strong>Username:</strong> {{Users.username}}</p>
             <p>
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
